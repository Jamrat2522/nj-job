# NJ HR — สรุปไฟล์ทั้งหมด และสถานะงาน (อัปเดตล่าสุด)

## ✅ เสร็จแล้ว

**1. Phase 1 — เพิ่มความเร็ว (วัดผลจริง ไม่ใช่ประมาณ)**
อยู่ในไฟล์ `app.js` / `index.html` ของโปรเจกต์ (นอกโฟลเดอร์นี้)

| รายการทดสอบ (150 คน · 5,000 ใบลา) | ก่อน | หลัง | เปลี่ยน |
|---|---|---|---|
| เปิดหน้าอนุมัติ | 3,835 ms | 224 ms | **−94%** |
| อนุมัติคำขอ | 4,137 ms | 282 ms | **−93%** |
| ส่งคำขอลา | 699 ms | 222 ms | −68% |
| Login → Dashboard | 751 ms | 189 ms | −75% |
| เปิด Timeline | 135 ms | 35 ms | −74% |

สิ่งที่แก้: index cache แทน scan array ในลูป · ตัดหน่วงเทียม 350/450/550 ms ·
เรนเดอร์การ์ดเป็นช่วง + event delegation · อนุมัติแล้วอัปเดตเฉพาะการ์ด ·
ไม่สร้าง sidebar/topbar ใหม่ตอนเปลี่ยนหน้า · lazy-load master-salary.js · ลดน้ำหนักฟอนต์
Regression 18/18 ผ่าน · ไม่มี JS error

**2. เชื่อม `app_users.employee_id` = 57 คน** (ตรวจด้วยตาครบทุกแถว ไม่มีผิด)
ย้อนกลับได้จากตาราง `njhr_link_backup_20260727`

## ⏸ รอการตัดสินใจ (เขียนต่อไม่ได้ถ้าไม่มีคำตอบ)

1. **49 คนไม่มีระเบียนพนักงาน + 3 บัญชี kanyarat** — พนักงานจริง / ลาออก / บัญชีซ้ำ?
2. **ระบบ auth ทาง A หรือ B** — `auth_id` มีแค่ 2/362 คน ทำให้ RLS ใช้ไม่ได้
   และยังลบ `nj_v6_anon_all` ไม่ได้ (ข้อมูลเงินเดือน/บัตรประชาชนยังเปิดอยู่)

---

## ไฟล์ในโฟลเดอร์นี้

| ไฟล์ | ใช้ทำอะไร | สถานะ |
|---|---|---|
| `00_INDEX.md` | ไฟล์นี้ | — |
| `00_READ_FIRST.md` | อธิบายว่าทำไม SQL ชุดแรกพัง | อ่านประกอบ |
| `00_inspect.sql` / `00_inspect_oneshot.sql` | ตรวจโครงสร้าง Supabase (อ่านอย่างเดียว) | ✅ ใช้แล้ว |
| `00_inspect_step2.sql` / `00_inspect_step3.sql` | ตรวจตาราง HR + ฟังก์ชันสิทธิ์ | ✅ ใช้แล้ว |
| `10_GAP_ANALYSIS.md` | ตารางเทียบ prototype ↔ Supabase ของจริง | 📌 สำคัญ |
| `11_AUTH_FINDINGS.md` | ต้นตอปัญหาสิทธิ์ + ทางเลือก A/B/C | 📌 ต้องตัดสินใจ |
| `12_link_preview.sql` / `13_link_preview2.sql` | ทดลองจับคู่ผู้ใช้↔พนักงาน | ✅ ใช้แล้ว |
| `14_mapping_worksheet.sql` / `15_mapping_v2.sql` | ใบตรวจให้ HR ยืนยัน (Export CSV ได้) | 🔜 ใช้กับ 54 คนที่เหลือ |
| `20_link_employee_id.sql` | เชื่อม employee_id (สำรอง→dry run→เขียน→rollback) | ✅ รันแล้ว 57 คน |
| `01_schema.sql` `02_rls.sql` `03_functions.sql` | ⛔ **ห้ามรัน** — เขียนตอนยังไม่รู้ว่ามีระบบเดิม | ยกเลิก |
| `04_migrate.js` | migration localStorage→Supabase | ⏸ ต้องเขียนใหม่ตามโครงจริง |
| `05_ROLLBACK.md` | วิธีย้อนกลับทุกขั้นตอน | 📌 เก็บไว้ |

## ลำดับงานที่เหลือ (เมื่อได้คำตอบ 2 ข้อ)

1. เชื่อม employee_id ที่เหลือ (2 + 3 + 49 คน) ตามคำตอบ HR
2. ตั้งค่า auth ตามทางที่เลือก → เปิดใช้ RLS ที่มีอยู่ → ลบ `nj_v6_anon_all` อย่างปลอดภัย
3. ต่อ prototype เข้ากับ `leave_requests` / `leave_attachments` / `work_shifts` ของจริง
4. Migration ข้อมูลลาจาก localStorage (ถ้าต้องการเก็บ)
5. Benchmark + ทดสอบสิทธิ์ทุก Role อีกรอบบนข้อมูลจริง
