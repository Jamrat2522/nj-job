/* MASSENGER V3 · utils/format.js — pure formatters (Phase 3 safe util split)
   classic script · โหลดก่อน core/runtime.js + app.js · ฟังก์ชัน global เดิม (ไม่เปลี่ยนชื่อ)
   ย้ายจาก app.js: esc, fmtDate, fmtDateTime, _fmtBytes, _fmtDtShort (pure · ไม่มี dependency ภายนอก) */
function esc(s){if(s===null||s===undefined)return"";return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}function fmtDate(ts){if(!ts)return"—";return new Date(ts).toLocaleDateString("th-TH",{day:"2-digit",month:"short"})}function fmtDateTime(ts){if(!ts)return"—";return new Date(ts).toLocaleString("th-TH",{day:"2-digit",month:"short",hour:"2-digit",minute:"2-digit"})}function _fmtBytes(b){b=Number(b)||0;if(b<1024)return b+" B";if(b<1048576)return(b/1024).toFixed(1)+" KB";return(b/1048576).toFixed(2)+" MB"}function _fmtDtShort(iso){if(!iso)return"—";const d=new Date(iso);if(isNaN(d.getTime()))return"—";const _p=x=>String(x).padStart(2,"0");return`${_p(d.getDate())}/${_p(d.getMonth()+1)} ${_p(d.getHours())}:${_p(d.getMinutes())}`}
