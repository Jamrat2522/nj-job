# HR V2 — Production Integration Checklist (โหมด Preview · v2-preview-1)

> เงื่อนไขก่อนเริ่ม: SQL `96_version_control.sql` ติดตั้งแล้ว · อัปโหลด `hr-v2/` ขึ้น `https://<โดเมน>/hr-v2/` · **ห้ามทับไฟล์ V1 ใด ๆ**
> รอบนี้คือการทดสอบ Integration เท่านั้น — **ห้าม Cutover · ห้ามประกาศให้พนักงานใช้ · Write Lock ต้องล็อกอยู่ตลอด ยกเว้นช่วงทดสอบเขียนที่ระบุไว้ในข้อ 4**
> ผลทุกข้อให้บันทึกเป็น PASS / FAIL / NOT RUN ตามจริง — ห้ามนำผล Mock มานับแทน

## 0) ตรวจก่อนอัปโหลด (กันชน V1)
- [ ] 0.1 ยืนยันตำแหน่งติดตั้ง V1 (root `/` หรือ subfolder) — บันทึกไว้: ______
- [ ] 0.2 ถ้า V1 อยู่ที่ root: หลังอัปโหลด V2 ให้เปิด `/hr-v2/` → `#/system` → ช่อง "SW ที่ควบคุมหน้านี้"
      · ขึ้น "ไม่มี" = ปลอดภัย · ขึ้น `sw.js` ของ V1 = แจ้งกลับก่อนทดสอบต่อ (ห้าม unregister เอง — ดู PREVIEW-RULES.md)
- [ ] 0.3 ช่อง "SW ของ V2" ต้องเป็น "ไม่มี (ถูกต้อง)"
- [ ] 0.4 ตรวจ Feature Flag: `select value->'v2_access' from public.system_settings where key='njhr_release';`
      ต้องได้ `roles:["SUPER_ADMIN"], users:[...เฉพาะผู้ทดสอบ]`

## 1) Deploy (ลำดับบังคับ — กฎ 40/41/47)
- [ ] 1.1 อัปโหลดไฟล์ `hr-v2/` ครบทุกไฟล์ (ตรวจจำนวนไฟล์ตรงกับ FILES.txt)
- [ ] 1.2 เปิด `https://<โดเมน>/hr-v2/` ใน Chrome เครื่อง Desktop — เห็นหน้า Login (ไม่มี error ใน Console)
- [ ] 1.3 SUPER_ADMIN (jamrat) login สำเร็จ → เข้าหน้า #/system ได้ เห็นเวอร์ชัน `v2-preview-1`
- [ ] 1.4 ทุกหน้าต้องมีแถบเหลือง "PREVIEW — ระบบทดสอบ ยังไม่เปิดใช้งานจริง" และสถานะ "ล็อกการบันทึกข้อมูล"
- [ ] 1.5 ที่ #/system กด "ประกาศเวอร์ชันใหม่" เป็น `v2-preview-1` — ระบบต้องไม่เข้า Maintenance เพราะไฟล์ตรงเวอร์ชันแล้ว

## 2) Auth + Feature Flag
- [ ] 2.1 Login บัญชี role EMPLOYEE ที่ยังไม่อยู่ในรายชื่อทดสอบ → เห็นหน้า "ยังไม่เปิดใช้งาน"
- [ ] 2.2 เพิ่ม user ทดสอบเข้า `v2_access.users` (ผ่านหน้า #/system) → login ใหม่เข้าได้
- [ ] 2.3 V1 เดิมยังใช้งานปกติ (เปิด V1 login ได้ ข้อมูลครบ) — **สำคัญที่สุด**

## 3) อ่านข้อมูลจริง (ยังไม่เขียน)
- [ ] 3.1 Dashboard: ตัวเลขตรงกับความเป็นจริง (เทียบกับ V1)
- [ ] 3.2 พนักงาน: รายชื่อ/จำนวน ตรงกับ V1 · รหัส 0xxx ศูนย์นำหน้าครบ
- [ ] 3.3 ลางาน: สิทธิ์คงเหลือ ตรงกับ V1
- [ ] 3.4 สลิป: EMPLOYEE เห็นเฉพาะของตัวเอง · ACCOUNT เห็นทั้งหมด
- [ ] 3.5 รายงานลงเวลา 1 เดือน: จำนวนแถวตรงกับ V1

## 4) เขียนข้อมูล — **เฉพาะ Module ที่ผ่านข้อ 3 แล้วเท่านั้น** (เงื่อนไขข้อ 5 ของคุณ)
ขั้นตอนบังคับต่อ 1 Module: ① ตรวจอ่านข้อมูลตรงกับ V1 → ② `#/system` กด "ปลดล็อกการบันทึก (ทดสอบ)" →
③ ทดสอบเขียน 1 รายการ → ④ ตรวจผลใน V1 → ⑤ ลบข้อมูลทดสอบ → ⑥ กด "ล็อกการบันทึกกลับ" ทันที
- [ ] 4.0 ยืนยันว่าขณะล็อกอยู่ กดปุ่มบันทึกใด ๆ แล้วขึ้นข้อความ "โหมด Preview: ล็อกการบันทึกข้อมูลจริงไว้" และ **ไม่มี request ออกไป** (ดู Network tab)
- [ ] 4.1 ยื่นใบลาทดสอบ 1 ใบ → เห็นใน V1 ถูกต้อง → อนุมัติจาก V2 → สถานะตรงทั้งสองฝั่ง → ยกเลิก/ลบข้อมูลทดสอบ
- [ ] 4.2 ส่ง OT ทดสอบ 1 รายการ (njhr_ot_submit) → ตรวจแถวใน DB → อนุมัติ → ลบข้อมูลทดสอบ
- [ ] 4.3 ลงเวลาเข้า-ออกด้วยมือถือจริงในพื้นที่ geofence → เวลาบันทึกเป็นเวลาเซิร์ฟเวอร์
- [ ] 4.4 นอกพื้นที่ geofence → ระบบปฏิเสธพร้อมข้อความ

## 5) Force Update + Maintenance (กฎ 45)
- [ ] 5.1 เปิด V2 ค้างไว้ 2 อุปกรณ์ (Desktop+มือถือ) → SUPER_ADMIN กด "เริ่ม Maintenance 10 นาที" → ทั้ง 2 เครื่องถูกบังคับออกภายใน ~3 นาทีหรือทันทีเมื่อสลับแท็บ → เห็นหน้านับถอยหลัง
- [ ] 5.2 ระหว่าง Maintenance: login บัญชีธรรมดาไม่ได้ · SUPER_ADMIN เข้าได้ (bypass)
- [ ] 5.3 กด "ปิด Maintenance" → เครื่องที่ค้างหน้านับถอยหลังกลับสู่หน้า Login เอง โดยไม่ต้อง refresh
- [ ] 5.4 ทดสอบ Force Update จริง **เฉพาะบัญชี/อุปกรณ์ทดสอบ**: แก้ `NJHR_V2_BUILD` เป็น `v2-preview-2` → อัปโหลด index.html ใหม่ → กดประกาศเวอร์ชัน `v2-preview-2` ที่ #/system → เครื่องที่เปิดค้างถูกบังคับออก + โหลดไฟล์ใหม่ (ดู Network: bootstrap.js?v=v2-preview-2)
- [ ] 5.6 ระหว่างทดสอบทั้งหมด V1 ยังใช้งานได้ตามปกติ (เปิด V1 คู่ขนานตรวจซ้ำ)
- [ ] 5.5 Read-only: เริ่ม Maintenance โหมด readonly → ใช้งานอ่านได้ แถบเหลืองขึ้น กดบันทึกใด ๆ ถูกบล็อก

## 6) มือถือ
- [ ] 6.1 เมนู hamburger เปิด-ปิดลื่น · ตารางแสดงเป็นการ์ด · ฟอร์ม Modal เต็มจอ · ปุ่มกดง่าย (≥44px)

## 7) ปิดท้าย
- [ ] 7.1 ลบข้อมูลทดสอบทั้งหมด (ใบลา/OT/พนักงานทดสอบ/ผู้ใช้ทดสอบ/แผนกทดสอบ)
- [ ] 7.2 ยืนยัน Write Lock กลับมาล็อกทุกเครื่อง
- [ ] 7.3 บันทึกผลแต่ละข้อ PASS/FAIL — ข้อที่ FAIL: ปิด Feature Flag ทันที (SQL ใน PREVIEW-RULES.md) แล้วส่ง error + screenshot Console กลับมา แก้เฉพาะฝั่ง V2
- [ ] 7.4 **ห้ามประกาศให้พนักงานใช้งาน** จนกว่า R1–R8 เสร็จและคุณอนุมัติ Cutover
